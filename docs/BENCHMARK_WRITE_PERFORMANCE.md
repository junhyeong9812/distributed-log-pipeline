# Write Performance 벤치마크

> 자원 한정 환경에서 PostgreSQL(단일 DB) vs HDFS/Spark(분산 처리)의 쓰기 성능 비교

---

## 📋 테스트 목적

### 핵심 질문

> **제한된 서버 자원에서 대용량 데이터를 안정적으로 저장할 수 있는가?**

### 검증 항목

1. **Write 처리량**: 초당/분당 저장 가능한 레코드 수
2. **안정성**: 자원 한계 상황에서 장애 발생 여부
3. **확장성**: 노드 추가로 처리량 증가 여부
4. **자원 효율성**: CPU/메모리 사용 패턴

---

## 🎯 가설

### 메인 가설

> **자원이 한정된 환경에서 분산 처리(HDFS/Spark)가 단일 DB(PostgreSQL)보다 대용량 데이터를 안정적으로 처리할 수 있다.**

### 세부 가설

| 상황 | PostgreSQL (단일) | HDFS/Spark (분산) |
|------|-------------------|-------------------|
| 소량 (1만건/분) | ✅ 안정적 | ✅ 안정적 (오버헤드 있음) |
| 중량 (10만건/분) | ⚠️ 부하 증가 | ✅ 분산 처리 |
| 대량 (100만건/분) | ❌ 병목/OOM 가능 | ✅ 수평 확장 이점 |

### 가설 근거

```
┌─────────────────────────────────────────────────────────────────────┐
│                    단일 DB vs 분산 시스템                            │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  PostgreSQL (단일 노드)                                             │
│  ├── 모든 쓰기가 하나의 노드에서 처리                                │
│  ├── 메모리 한계 → 버퍼 풀 부족 → 디스크 I/O 증가                   │
│  ├── CPU 한계 → 트랜잭션 처리 지연                                  │
│  ├── 인덱스 업데이트 오버헤드                                       │
│  └── 단일 장애점 (SPOF)                                             │
│                                                                      │
│  HDFS/Spark (분산)                                                  │
│  ├── 데이터가 여러 노드에 분산 저장                                  │
│  ├── 각 노드가 독립적으로 처리                                      │
│  ├── 노드 추가로 처리량 증가                                        │
│  ├── Kafka 버퍼로 순간 부하 흡수                                    │
│  └── 복제로 장애 내성                                               │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 🖥️ 테스트 환경

### 하드웨어 구성

| 노드 | IP | 실제 사양 | 테스트 제한 |
|------|-----|----------|------------|
| Master | 192.168.55.114 | 8코어/16GB | - |
| Worker 1 | 192.168.55.158 | 6코어/16GB | 2코어/2GB |
| Worker 2 | 192.168.55.9 | 8코어/16GB | 2코어/2GB |

### 리소스 제한 설정

```yaml
# PostgreSQL
resources:
  limits:
    cpu: "2"
    memory: "2Gi"
  requests:
    cpu: "1"
    memory: "1Gi"

# HDFS DataNode (각 Worker)
resources:
  limits:
    cpu: "2"
    memory: "2Gi"
  requests:
    cpu: "1"
    memory: "1Gi"

# Spark Worker (각 Worker)
resources:
  limits:
    cpu: "2"
    memory: "2Gi"
  requests:
    cpu: "1"
    memory: "1Gi"
```

### 총 자원 비교

| 시스템 | 총 CPU | 총 메모리 | 노드 수 |
|--------|--------|----------|--------|
| PostgreSQL | 2코어 | 2GB | 1 |
| HDFS/Spark | 4코어 (2+2) | 4GB (2+2) | 2 |

> **참고**: 분산 시스템이 2배 자원을 사용하지만, 이것이 수평 확장의 핵심 이점

---

## 📊 테스트 시나리오

### 데이터 흐름

```
┌─────────────────────────────────────────────────────────────────────┐
│                         Write Path                                   │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│                        Generator                                     │
│                           │                                          │
│                           ▼                                          │
│                        Backend                                       │
│                           │                                          │
│              ┌────────────┴────────────┐                            │
│              ▼                         ▼                            │
│         PostgreSQL                   Kafka                          │
│         (직접 저장)                    │                            │
│              │                         ▼                            │
│              │                   Spark Streaming                    │
│              │                         │                            │
│              │                         ▼                            │
│              │                       HDFS                           │
│              │                    (분산 저장)                        │
│              ▼                         ▼                            │
│         [측정 지점]              [측정 지점]                         │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
```

### Phase 1: 기본 부하 (1만건/분)

```
설정:
- BATCH_SIZE: 100
- INTERVAL: 0.6초 (100건/0.6초 ≈ 10,000건/분)
- 지속 시간: 10분
- 총 데이터: 약 10만건

측정:
- 저장 성공률
- 평균 응답 시간
- CPU/메모리 사용량
- 에러 발생 여부
```

### Phase 2: 중간 부하 (10만건/분)

```
설정:
- BATCH_SIZE: 1000
- INTERVAL: 0.6초 (1000건/0.6초 ≈ 100,000건/분)
- 지속 시간: 10분
- 총 데이터: 약 100만건

측정:
- 저장 성공률
- 응답 시간 변화
- 자원 사용량 추이
- 병목 지점 확인
```

### Phase 3: 고부하 (100만건/분)

```
설정:
- BATCH_SIZE: 5000
- INTERVAL: 0.3초 (5000건/0.3초 ≈ 1,000,000건/분)
- 지속 시간: 5분 (또는 장애 발생까지)
- 총 데이터: 약 500만건

측정:
- 최대 처리량 (한계점)
- 장애 발생 시점
- 복구 시간
- 데이터 유실 여부
```

---

## 📈 측정 지표

### Primary Metrics

| 지표 | 설명 | 단위 |
|------|------|------|
| Throughput | 초당 저장 레코드 수 | records/sec |
| Latency (P50) | 중간값 응답 시간 | ms |
| Latency (P99) | 99번째 백분위 응답 시간 | ms |
| Success Rate | 저장 성공률 | % |
| Error Count | 에러 발생 횟수 | count |

### Resource Metrics

| 지표 | 설명 | 측정 방법 |
|------|------|----------|
| CPU Usage | CPU 사용률 | Grafana/Prometheus |
| Memory Usage | 메모리 사용량 | Grafana/Prometheus |
| Disk I/O | 디스크 읽기/쓰기 | Grafana/Prometheus |
| Network I/O | 네트워크 트래픽 | Grafana/Prometheus |

### Stability Metrics

| 지표 | 설명 |
|------|------|
| OOM 발생 | Out of Memory 에러 |
| Pod Restart | 컨테이너 재시작 횟수 |
| Connection Timeout | 연결 시간 초과 |
| Data Loss | 데이터 유실 여부 |

---

## 🔧 구현 계획

### 1단계: 리소스 제한 적용

```bash
# PostgreSQL 리소스 제한
kubectl patch deployment postgres -n log-pipeline --patch '
spec:
  template:
    spec:
      containers:
      - name: postgres
        resources:
          limits:
            cpu: "2"
            memory: "2Gi"
          requests:
            cpu: "1"
            memory: "1Gi"
'

# DataNode 리소스 제한
kubectl patch daemonset datanode -n log-pipeline --patch '...'

# Spark Worker 리소스 제한
kubectl patch daemonset spark-worker -n log-pipeline --patch '...'
```

### 2단계: Generator 속도 조절

```python
# config.py 수정
class Settings(BaseSettings):
    batch_size: int = int(os.getenv("BATCH_SIZE", "100"))
    log_interval_seconds: float = float(os.getenv("LOG_INTERVAL", "5"))
    
    # 계산: batch_size / interval = records/sec
    # 100 / 5 = 20 records/sec = 1,200 records/min (기본)
    # 1000 / 0.6 = 1,667 records/sec = 100,000 records/min
    # 5000 / 0.3 = 16,667 records/sec = 1,000,000 records/min
```

### 3단계: 모니터링 대시보드

```
Grafana 대시보드:
- PostgreSQL 메트릭 (CPU, Memory, Connections, TPS)
- HDFS 메트릭 (DataNode 상태, 블록 수, 저장 용량)
- Spark 메트릭 (Worker 상태, 처리량)
- Kafka 메트릭 (Lag, Throughput)
```

### 4단계: 테스트 스크립트

```bash
#!/bin/bash
# write_benchmark.sh

# Phase 1: 기본 부하
echo "Phase 1: 1만건/분"
kubectl set env deployment/generator -n log-pipeline \
  BATCH_SIZE=100 LOG_INTERVAL=0.6
sleep 600  # 10분

# 메트릭 수집
curl "http://prometheus:9090/api/v1/query?query=..." > phase1_metrics.json

# Phase 2: 중간 부하
echo "Phase 2: 10만건/분"
kubectl set env deployment/generator -n log-pipeline \
  BATCH_SIZE=1000 LOG_INTERVAL=0.6
sleep 600

# Phase 3: 고부하
echo "Phase 3: 100만건/분"
kubectl set env deployment/generator -n log-pipeline \
  BATCH_SIZE=5000 LOG_INTERVAL=0.3
sleep 300
```

---

## 📋 예상 결과

### Phase 1 (1만건/분)

| 시스템 | Throughput | Latency | CPU | Memory | 상태 |
|--------|------------|---------|-----|--------|------|
| PostgreSQL | ~170/s | <50ms | 30% | 500MB | ✅ 안정 |
| HDFS/Spark | ~170/s | <100ms | 20%+20% | 400MB+400MB | ✅ 안정 |

### Phase 2 (10만건/분)

| 시스템 | Throughput | Latency | CPU | Memory | 상태 |
|--------|------------|---------|-----|--------|------|
| PostgreSQL | ~1,500/s | <200ms | 80% | 1.5GB | ⚠️ 부하 |
| HDFS/Spark | ~1,600/s | <150ms | 50%+50% | 1GB+1GB | ✅ 안정 |

### Phase 3 (100만건/분)

| 시스템 | Throughput | Latency | CPU | Memory | 상태 |
|--------|------------|---------|-----|--------|------|
| PostgreSQL | ~5,000/s? | >1000ms | 100% | 2GB (OOM?) | ❌ 한계 |
| HDFS/Spark | ~10,000/s | <500ms | 80%+80% | 1.5GB+1.5GB | ✅ 처리 |

---

## 🎯 성공 기준

### PostgreSQL

| 기준 | Phase 1 | Phase 2 | Phase 3 |
|------|---------|---------|---------|
| 성공률 | >99% | >95% | >90% |
| P99 Latency | <100ms | <500ms | <2000ms |
| OOM 발생 | 없음 | 없음 | 허용 |

### HDFS/Spark

| 기준 | Phase 1 | Phase 2 | Phase 3 |
|------|---------|---------|---------|
| 성공률 | >99% | >99% | >95% |
| P99 Latency | <200ms | <300ms | <1000ms |
| OOM 발생 | 없음 | 없음 | 없음 |

---

## 📊 결과 분석 프레임워크

### 비교 포인트

1. **처리량 한계**
    - PostgreSQL: 단일 노드 최대 처리량
    - HDFS/Spark: 분산 처리 최대 처리량
    - 차이 비율 계산

2. **안정성**
    - 장애 발생 시점 (데이터량 기준)
    - 복구 시간
    - 데이터 유실 여부

3. **확장성 증명**
    - PostgreSQL: 수직 확장만 가능 (비용 증가)
    - HDFS/Spark: 수평 확장 (노드 추가로 처리량 증가)

### 결론 템플릿

```
테스트 결과 요약:

1. 처리량
   - PostgreSQL 최대: X records/sec
   - HDFS/Spark 최대: Y records/sec
   - 분산 시스템이 Z배 높은 처리량

2. 안정성
   - PostgreSQL: N만건에서 장애 발생
   - HDFS/Spark: M만건까지 안정적 처리

3. 자원 효율성
   - PostgreSQL: CPU 100% 도달 시점
   - HDFS/Spark: 부하 분산으로 여유 유지

4. 결론
   - 소량 데이터: PostgreSQL 권장 (단순성)
   - 대용량 데이터: HDFS/Spark 권장 (확장성)
   - 교차점: 약 X만건/분
```

---

## 📅 실행 일정

| 단계 | 작업 | 예상 소요 |
|------|------|----------|
| 1 | K8s 리소스 제한 설정 | 30분 |
| 2 | Generator 속도 조절 기능 | 30분 |
| 3 | 모니터링 대시보드 확인 | 30분 |
| 4 | Phase 1 테스트 | 15분 |
| 5 | Phase 2 테스트 | 15분 |
| 6 | Phase 3 테스트 | 10분 |
| 7 | 결과 분석 및 문서화 | 1시간 |

---

## 🔗 관련 문서

- [BENCHMARK.md](BENCHMARK.md) - 조회 성능 비교 (1차 결과)
- [ARCHITECTURE.md](ARCHITECTURE.md) - 시스템 아키텍처
- [TROUBLESHOOTING.md](TROUBLESHOOTING.md) - 트러블슈팅 가이드